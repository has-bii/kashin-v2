{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "hono": "^4.12.18"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
